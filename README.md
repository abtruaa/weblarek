# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:

- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:

- `container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:

- `render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.
- `setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:

- `constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:

- `baseUrl: string` - базовый адрес сервера
- `options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:

- `get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер
- `post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.
- `handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:

- `_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:

- `on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.
- `emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.
- `trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

### Интерфейсы данных

#### Товар

```typescript
interface IItem {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```
Описывает структуру товара в каталоге.

#### Заказ

```typescript
interface IOrder {
  payment: string;
  address: string;
  email: string;
  phone: string;
  total: number;
  items: string[];
}
```
Описывает структуру заказа для отправки на сервер.

#### Результат оформления заказа

```typescript
interface IOrderResult {
  total: number;
}
```
Описывает структуру ответа сервера при успешном оформлении заказа.

### Модели данных

#### Класс ApiService
Класс для взаимодействия с API сервером, наследуется от базового класса Api.

**Конструктор:**
- `constructor(cdn: string, baseUrl: string, options?: RequestInit)` - принимает CDN URL для изображений, базовый URL API и опциональные настройки запросов.

**Поля класса:**
- `cdn: string` - базовый URL для изображений товаров

**Методы класса:**
- `getAllItems(): Promise<IItem[]>` - получает список всех товаров с сервера и преобразует пути к изображениям
- `getItemId(itemId: string): Promise<IItem>` - получает конкретный товар по ID
- `postItem(order: IOrder): Promise<IOrderResult>` - отправляет заказ на сервер

#### Класс Products
Модель для хранения каталога товаров.

**Конструктор:**
- `constructor(events: IEvents)` - принимает экземпляр EventEmitter для генерации событий

**Методы класса:**
- `saveProducts(products: IItem[]): void` - сохраняет массив товаров и генерирует событие `item:setAllItems`
- `getProducts(): IItem[]` - возвращает все товары из каталога
- `getProductById(id: string): IItem | undefined` - возвращает товар по ID
- `setSelected(item: IItem): void` - устанавливает выбранный товар для предпросмотра и генерирует событие `preview:open`
- `getSelected(): IItem | null` - возвращает текущий выбранный товар

#### Класс Cart
Модель корзины покупок.

**Конструктор:**
- `constructor(events: IEvents)` - принимает экземпляр EventEmitter для генерации событий

**Методы класса:**
- `addItem(itemToAdd: IItem): void` - добавляет товар в корзину и генерирует событие `basket:changed`
- `deleteItem(idToDelete: string): void` - удаляет товар из корзины по ID и генерирует событие `basket:changed`
- `getItems(): IItem[]` - возвращает все товары в корзине
- `clearCart(): void` - очищает корзину и генерирует событие `basket:changed`
- `getTotalCost(): number` - возвращает общую стоимость товаров в корзине
- `getTotalAmount(): number` - возвращает количество товаров в корзине
- `itemIsInCart(id: string): boolean` - проверяет, есть ли товар с указанным ID в корзине

#### Класс Customer
Модель данных покупателя с раздельной валидацией форм.

**Конструктор:**
- `constructor(events: IEvents)` - принимает экземпляр EventEmitter для генерации событий

**Методы класса:**
- `setOrderField(field: keyof IBuyer, value: string): void` - устанавливает значение поля и запускает валидацию, генерирует события `formErrors:change`, `order:validation`, `contacts:validation`
- `saveCustomerData(data: Partial<IBuyer>): void` - сохраняет несколько полей данных покупателя
- `validateOrderForm(): FormErrors` - валидирует только форму заказа (payment, address)
- `validateContactForm(): FormErrors` - валидирует только форму контактов (email, phone)
- `isOrderFormValid(): boolean` - проверяет валидность формы заказа
- `isContactsFormValid(): boolean` - проверяет валидность формы контактов
- `getAllCustomerData(): IBuyer` - возвращает все данные покупателя
- `clearCustomerData(): void` - очищает все данные покупателя
- `getErrors(): FormErrors` - возвращает текущие ошибки валидации
## Слой представления View

### Интерфейсы представления

#### Базовый интерфейс карточки товара

```typescript
interface ICardView {
  id: string;
  title: string;
  price: number | null;
}
```

#### Интерфейс формы

```typescript
interface IForm {
  isValid: boolean;
  errors: string[];
}
```

#### Интерфейс формы заказа

```typescript
interface IOrderForm {
  payment: string;
  address: string;
}
```

#### Интерфейс формы контактов

```typescript
interface IContactsForm {
  phone: string;
  email: string;
}
```

#### Интерфейс модального окна

```typescript
interface IModal {
  content: HTMLElement;
}
```

#### Интерфейс корзины

```typescript
interface IBasket {
  items: HTMLElement[];
  fullPrice: number;
}
```

#### Интерфейс успешного оформления

```typescript
interface ISuccess {
  total: number;
}
```

#### Интерфейс главной страницы

```typescript
interface IPage {
  itemList: HTMLElement[];
  counter: number;
  locked: boolean;
}
```

### Классы представления

#### Класс Item (базовый)

Базовый класс для карточки товара.

**Конструктор:**
- `constructor(container: HTMLElement, events?: IEvents, action?: IAction)`

**Поля класса:**
- `itemTitle: HTMLElement` - элемент заголовка товара
- `itemPrice: HTMLElement` - элемент цены товара

**Cеттеры:**
- `set price(price: number | null)` - устанавливает цену товара, при null отображает "Бесценно"
- `set title(value: string)` - устанавливает заголовок товара

#### Класс ItemElement

Карточка товара для галереи, наследуется от Item.

**Конструктор:**
- `constructor(container: HTMLElement, actions?: IItemActions)` - обработчики передаются через объект actions

**Дополнительные поля:**
- `itemImage: HTMLImageElement` - изображение товара
- `itemCategory: HTMLElement` - категория товара
- `button: HTMLButtonElement` - кнопка карточки

**Дополнительные сеттеры:**
- `set image(value: string)` - устанавливает изображение товара
- `set category(value: string)` - устанавливает категорию товара с соответствующим CSS классом

#### Класс ItemPreview

Карточка товара для предпросмотра в модальном окне, наследуется от ItemElement.

**Конструктор:**
- `constructor(container: HTMLElement, events?: IEvents, action?: IAction)`

**Дополнительные поля:**
- `itemDescription: HTMLElement` - описание товара
- `_itemButton: HTMLButtonElement` - кнопка "Купить"/"Убрать"

**Дополнительные сеттеры:**
- `set description(value: string)` - устанавливает описание товара
- `set itemButton(value: boolean)` - устанавливает текст кнопки (Купить/Убрать)

#### Класс BasketItem

Карточка товара в корзине, наследуется от Item.

**Конструктор:**
- `constructor(container: HTMLElement, events?: IEvents, action?: IAction)`

**Дополнительные поля:**
- `_index: HTMLElement` - индекс товара в корзине
- `buttonDelete: HTMLButtonElement` - кнопка удаления из корзины

**Дополнительные сеттеры:**
- `set index(value: number)` - устанавливает индекс товара в корзине

#### Класс Form (абстрактный)

Абстрактный базовый класс для всех форм.

**Конструктор:**
- `constructor(container: HTMLElement, events: IEvents)`

**Поля класса:**
- `buttonSubmit: HTMLButtonElement` - кнопка отправки формы
- `_errors: HTMLElement` - элемент для отображения ошибок

**Методы:**
- `protected onInputChange(field: keyof T, value: string)` - обработчик изменения поля формы
- `render(state: Partial<T> & IForm)` - рендеринг формы с данными

**Сеттеры:**
- `set isValid(value: boolean)` - активирует/деактивирует кнопку отправки
- `set errors(value: string)` - устанавливает текст ошибок

#### Класс OrderForm

Форма оформления заказа, наследуется от Form<IOrderForm>.

**Конструктор:**
- `constructor(container: HTMLFormElement, events: IEvents)`

**Дополнительные поля:**
- `_buttons: HTMLButtonElement[]` - кнопки выбора способа оплаты

**Сеттеры:**
- `set payment(name: string)` - устанавливает выбранный способ оплаты
- `set address(value: string)` - устанавливает адрес доставки

#### Класс ContactForm

Форма контактных данных, наследуется от Form<IContactsForm>.

**Конструктор:**
- `constructor(container: HTMLFormElement, events: IEvents)`

**Сеттеры:**
- `set phone(value: string)` - устанавливает номер телефона
- `set email(value: string)` - устанавливает email

#### Класс Modal

Модальное окно для отображения различного контента.

**Конструктор:**
- `constructor(container: HTMLElement, events: IEvents)`

**Поля класса:**
- `closeButton: HTMLButtonElement` - кнопка закрытия модального окна
- `_content: HTMLElement` - контейнер для содержимого

**Методы:**
- `open()` - открывает модальное окно
- `close()` - закрывает модальное окно

**Сеттеры:**
- `set content(value: HTMLElement)` - устанавливает содержимое модального окна

#### Класс Page

Главная страница приложения.

**Конструктор:**
- `constructor(container: HTMLElement, events: IEvents)`

**Поля класса:**
- `headerBasketButton: HTMLButtonElement` - кнопка корзины в хедере
- `headerBasketCounter: HTMLElement` - счетчик товаров в корзине
- `itemContainer: HTMLElement` - контейнер для списка товаров
- `wrapper: HTMLElement` - обертка страницы для блокировки скролла

**Сеттеры:**
- `set itemList(items: HTMLElement[])` - устанавливает список товаров в галерее
- `set counter(value: number)` - устанавливает счетчик товаров в корзине
- `set locked(value: boolean)` - блокирует/разблокирует скролл страницы

#### Класс Basket

Модальное окно корзины.

**Конструктор:**
- `constructor(container: HTMLElement, events: IEvents)`

**Поля класса:**
- `basketList: HTMLUListElement` - список товаров в корзине
- `basketButton: HTMLButtonElement` - кнопка оформления заказа
- `basketPrice: HTMLElement` - элемент общей стоимости

**Сеттеры:**
- `set items(items: HTMLElement[])` - устанавливает список товаров в корзине
- `set fullPrice(value: number)` - устанавливает общую стоимость заказа

#### Класс Success

Окно успешного оформления заказа.

**Конструктор:**
- `constructor(container: HTMLElement, action?: ISuccessActions)`

**Поля класса:**
- `close: HTMLElement` - кнопка закрытия окна
- `_total: HTMLElement` - элемент с информацией о списанной сумме

**Сеттеры:**
- `set total(value: number)` - устанавливает информацию о списанной сумме

## Генерация событий

### События товаров

#### `item:setAllItems`
- **Генерирует**: Products (при загрузке товаров с сервера)
- **Данные**: нет
- **Описание**: Товары загружены и готовы к отображению
- **Обработка в Презентере**: Создать карточки товаров для галереи

#### `preview:open`
- **Генерирует**: Products (при выборе товара для предпросмотра)
- **Данные**: нет
- **Описание**: Пользователь выбрал товар для детального просмотра
- **Обработка в Презентере**: Открыть модальное окно с детальной информацией о товаре

### События корзины

#### `basket:open`
- **Генерирует**: Page (при клике на иконку корзины в хедере)
- **Данные**: нет
- **Описание**: Пользователь хочет открыть корзину
- **Обработка в Презентере**: Открыть модальное окно с содержимым корзины

#### `basket:continue`
- **Генерирует**: Basket (при клике на кнопку оформления заказа в корзине)
- **Данные**: нет
- **Описание**: Пользователь хочет оформить заказ
- **Обработка в Презентере**: Открыть форму оформления заказа

#### `basket:changed`
- **Генерирует**: Презентер (при любом изменении состояния корзины)
- **Данные**: нет
- **Описание**: Состояние корзины изменилось
- **Обработка в Презентере**: Обновить интерфейс корзины и счетчик

### События форм

#### Валидация форм
Приложение использует раздельную валидацию:
- **Форма заказа** (`order`) - валидирует только `payment` и `address`
- **Форма контактов** (`contacts`) - валидирует только `email` и `phone`

Это позволяет поэтапно заполнять форму заказа без необходимости сразу указывать контактные данные.

#### `order:validation`
- **Генерирует**: Customer (при изменении полей формы заказа)
- **Данные**: `{ isValid: boolean }` - результат валидации формы заказа
- **Описание**: Обновляет состояние валидности формы заказа
- **Обработка в Презентере**: Активирует/деактивирует кнопку "Далее"

#### `contacts:validation`
- **Генерирует**: Customer (при изменении полей формы контактов)
- **Данные**: `{ isValid: boolean }` - результат валидации формы контактов
- **Описание**: Обновляет состояние валидности формы контактов
- **Обработка в Презентере**: Активирует/деактивирует кнопку "Оплатить"

#### `formErrors:change`
- **Генерирует**: Customer (при изменении любых полей форм)
- **Данные**: `FormErrors` - объект с ошибками валидации
- **Описание**: Обновляет сообщения об ошибках в формах
- **Обработка в Презентере**: Отображает ошибки пользователю

#### `order:submit`
- **Генерирует**: OrderForm (при отправке формы заказа)
- **Данные**: нет
- **Описание**: Пользователь заполнил форму заказа
- **Обработка в Презентере**: Сохранить данные заказа, открыть форму контактов

#### `contacts:submit`
- **Генерирует**: ContactForm (при отправке формы контактов)
- **Данные**: нет
- **Описание**: Пользователь заполнил контактные данные
- **Обработка в Презентере**: Отправить заказ на сервер, показать окно успеха

#### `order.payment:change`
- **Генерирует**: OrderForm (при изменении способа оплаты)
- **Данные**: `{ field: string, value: string }` - поле и значение
- **Описание**: Пользователь выбрал способ оплаты
- **Обработка в Презентере**: Сохранить способ оплаты, провести валидацию

#### `order.address:change`
- **Генерирует**: OrderForm (при изменении адреса)
- **Данные**: `{ field: string, value: string }` - поле и значение
- **Описание**: Пользователь ввел адрес доставки
- **Обработка в Презентере**: Сохранить адрес, провести валидацию

#### `contacts.email:change`
- **Генерирует**: ContactForm (при изменении email)
- **Данные**: `{ field: string, value: string }` - поле и значение
- **Описание**: Пользователь ввел email
- **Обработка в Презентере**: Сохранить email, провести валидацию

#### `contacts.phone:change`
- **Генерирует**: ContactForm (при изменении телефона)
- **Данные**: `{ field: string, value: string }` - поле и значение
- **Описание**: Пользователь ввел телефон
- **Обработка в Презентере**: Сохранить телефон, провести валидацию

### События модальных окон

#### `modal:open`
- **Генерирует**: Modal (при открытии модального окна)
- **Данные**: нет
- **Описание**: Модальное окно открыто
- **Обработка в Презентере**: Заблокировать скролл страницы

#### `modal:close`
- **Генерирует**: Modal (при закрытии модального окна)
- **Данные**: нет
- **Описание**: Модальное окно закрыто
- **Обработка в Презентере**: Разблокировать скролл страницы

### Действия (Actions)

#### Интерфейс IAction
```typescript
interface IAction {
  onClick: (event: MouseEvent) => void;
}
```
Используется для передачи обработчиков кликов в компоненты карточек.

#### Интерфейс ISuccessActions
```typescript
interface ISuccessActions {
  onClick: () => void;
}
```
Используется для передачи обработчика закрытия окна успешного оформления заказа.

## Презентер (main.ts)

Основная логика приложения находится в файле `src/main.ts`, который выполняет роль презентера:

1. **Инициализация компонентов**: создает экземпляры всех View-компонентов
2. **Подписка на события**: регистрирует обработчики для всех событий приложения
3. **Управление состоянием**: хранит состояние корзины и данных заказа
4. **Координация взаимодействий**: связывает пользовательские действия с обновлением интерфейса и данными
5. **Работа с API**: управляет загрузкой товаров и отправкой заказов

Ключевые состояния приложения:
- `items: IItem[]` - все доступные товары
- `basketItems: IItem[]` - товары в корзине
- `order: Partial<IOrder>` - данные заказа

## Утилиты

### Файл utils.ts

Содержит вспомогательные функции:

- `ensureElement<T>(selector: string, parent?: HTMLElement): T` - гарантированно возвращает DOM элемент или выбрасывает ошибку
- `ensureAllElements<T>(selector: string, parent?: HTMLElement): T[]` - возвращает все элементы по селектору
- `cloneTemplate<T extends HTMLElement>(template: HTMLTemplateElement): T` - клонирует шаблон
- `createElement<T extends HTMLElement>(tagName: string, props?: object): T` - создает DOM элемент
- `isEmpty(value: any): boolean` - проверяет значение на пустоту

### Файл constants.ts

Содержит константы приложения:

- `API_URL` - базовый URL API сервера
- `CDN_URL` - базовый URL для изображений товаров